# PostgreSQL connection used by Prisma. Replace all placeholder values locally.
DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/myscreentime?schema=public"

# Used to encrypt authentication sessions when authentication is added in Phase 2.
AUTH_SECRET="replace-with-a-long-random-value"

# Public application URL.
NEXT_PUBLIC_APP_URL="http://localhost:3000"
